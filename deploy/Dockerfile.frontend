# этап 1: собираем фронтенд
FROM node:22-alpine AS build
WORKDIR /app
COPY front/package.json front/package-lock.json ./
RUN npm ci
COPY front/ ./
RUN npm run build

# этап 2: отдаём статику через nginx
FROM nginx:1.27-alpine
COPY --from=build /app/dist /usr/share/nginx/html